class UserActionsConflictsGroups {
    class ActionGroups {
        PREFIX[] = {
            QGVAR(battleMenu),
            QGVAR(artyMenu),
            QGVAR(infoBar),
            QGVAR(earPlugs),
            QGVAR(customHintDismiss)
        };
    };

    class CollisionGroups {
        ADDON[] = {QUOTE(PREFIX)};
    };
};
