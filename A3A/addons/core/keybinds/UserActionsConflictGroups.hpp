class UserActionsConflictsGroups {
    class ActionGroups {
        PREFIX[] = {
            QGVAR(battleMenu),
            QGVAR(artyMenu),
            QGVAR(infoBar),
            QGVAR(earPlugs)
        };
    };

    class CollisionGroups {
        ADDON[] = {QUOTE(PREFIX)};
    };
};
